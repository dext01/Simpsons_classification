# 🎬 Simpsons Classification (Lab 1)
## Классификация персонажей мультсериала "Симпсоны" с использованием свёрточной нейронной сети ResNet18 (Transfer Learning, Fine-tuning).
### 📋 Описание задачи
```Цель: Построить модель для автоматической классификации изображений персонажей мультсериала "Симпсоны".
Датасет: The Simpsons Characters Dataset

Архитектура: ResNet18 с заменой последнего слоя (full fine-tuning)

    Предобученные веса: ImageNet (IMAGENET1K_V1)
    Все слои обучаются (не замораживаются)
    Функция активации выхода: Softmax
    Функция потерь: CrossEntropyLoss
    Оптимизатор: Adam (lr=1e-4)
```

### 🚀 Быстрый старт
#### 1. Установка зависимостей
```
# Создаём виртуальное окружение
python3 -m venv venv

# Активируем окружение
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Обновляем pip и устанавливаем зависимости
pip install --upgrade pip
pip install -r requirements.txt
```
### 2. Подготовка датасета
```commandline
    Скачайте датасет с Kaggle: The Simpsons Characters Dataset
    Распакуйте архив
    Поместите папку с изображениями в проект:
    
    Simpsons_classification/
    └── data/
        ├── abraham_grampa_simpson/
        ├── agnes_skinner/
        ├── apu_nahasapeemapetilon/
        ├── ...
        └── (всего 42 папки с персонажами)
```

### 3. Запуск
```
Виртуальное окружение:
python3 -m venv venv
source venv/bin/activate

Установка зависимостей:
pip install --upgrade pip
pip install -r requirements.txt

Запускаем обучение:

Валидация данных:

```